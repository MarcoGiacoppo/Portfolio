/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['avatarfiles.alphacoders.com', 'pbs.twimg.com', 'assets.aceternity.com'], // Add the external domains you want to allow
    },
  };
  
  export default nextConfig;
  